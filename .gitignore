node_modules/
# dist/ is committed so `bun add github:…` / git installs work without a build
# step (Bun doesn't run the `prepare` script for git dependencies).
*.log
.env
.DS_Store
coverage/
.idea/
.vscode/
